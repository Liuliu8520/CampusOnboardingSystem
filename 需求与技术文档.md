# 高校迎新管理系统 需求规格说明书 & 技术设计文档

| 项目名称 | 高校迎新管理系统 (Campus Onboarding System) |
| :--- | :--- |
| 版本 | V1.0 |
| 开发模式 | 前后端分离 |
| 文档日期 | 2026-07-19 |

---

## 1. 项目概述

### 1.1 项目背景
传统高校迎新流程涉及缴费、宿舍分配、资格核验等多个线下环节，流程繁琐且易出错。本项目旨在构建一个线上化、自动化的迎新管理平台，实现从入学资格确认到最终报到入住的全流程闭环管理。

### 1.2 项目目标
- **自动化分配**：根据学生专业属性，实现宿舍的智能化、先到先得分配。
- **流程可视**：为学生提供清晰的四步报到引导（核验→缴费→选宿→报到）。
- **管理集约**：为管理员提供统一的6大核心模块，掌握迎新进度与数据统计。

---

## 2. 系统角色与功能需求

### 2.1 学生端功能
| 功能模块 | 详细描述 |
| :--- | :--- |
| **登录认证** | 使用学号+密码登录（验证码暂用随机数学公式结果如`10-7=?`，预留Kaptcha接口）。 |
| **资格核验** | 展示个人录取/分班信息。若信息有误，可提交“字段修改申请”（包含新值与原因）。 |
| **在线缴费** | 查看学费、住宿费等必缴/选缴项目列表，点击“模拟支付”完成缴费（无真实资金流）。 |
| **宿舍分配** | 缴费成功后，点击按钮触发**自动分配算法**（同专业优先填满现有房间），展示楼栋-房间-床位详情。 |
| **现场报到** | 抵达校园后，点击“确认到校报到”按钮，完成最终状态变更。 |
| **个人中心** | 查看个人基本信息及当前申请审核状态。 |
| **公告查看** | 查看管理员发布的最新通知。 |

### 2.2 管理员端功能（单级权限）
| 功能模块 | 详细描述（CRUD指增删改查） |
| :--- | :--- |
| **学生管理** | 列表筛选（学院/专业/报到状态），编辑学生信息，重置密码。 |
| **宿舍管理** | 维护三级结构（楼栋→房间→床位），支持批量生成房间，查看床位占用图。 |
| **缴费项目管理** | 配置学费/住宿费等项目的名称、金额及必缴属性。 |
| **资格修改审核** | 查看学生提交的修改申请表，操作“通过”（自动同步学生表）或“驳回”（填写原因）。 |
| **公告管理** | 发布、编辑、删除系统公告。 |
| **数据统计看板** | 展示总人数、报到率、缴费率；图表展示各学院报到率及每日缴费趋势。 |

---

## 3. 非功能性需求 (NFR)

- **性能**：支持至少 **5000名** 新生同时在线操作，核心接口响应时间 < 500ms。
- **安全**：密码采用 **BCrypt** 加密存储，防止数据泄露。
- **可用性**：前端采用响应式布局，兼容主流浏览器（Chrome/Edge/Firefox）。
- **扩展性**：宿舍分配算法模块化设计，便于后期调整分配策略（如混住规则）。

---

## 4. 技术架构选型 (Technical Stack)

| 层次 | 技术选型 | 版本/说明 |
| :--- | :--- | :--- |
| **前端框架** | Vue 3 + TypeScript | 组合式API (`<script setup>`) |
| **UI组件库** | Element Plus | 后台管理及学生端基础组件 |
| **构建工具** | Vite | 快速冷启动与热更新 |
| **后端框架** | Spring Boot | 3.x 版本 |
| **ORM框架** | MyBatis-Plus | 3.5.x，简化数据访问层开发 |
| **数据库** | MySQL | 8.0 版本 |
| **接口风格** | RESTful API | 统一返回 `Result<T>` 格式 |
| **跨域处理** | 前端代理 (Proxy) | Vite配置转发 `/api` 至后端 `8080` 端口 |

---

## 5. 数据库核心设计 (ER简图 & 关键表结构)

系统核心共涉及 **10张表**，遵循 `snake_case` 命名规范，均含 `create_time`/`update_time` 自动时间戳。

### 核心表结构说明

| 表名 | 核心字段 | 作用说明 |
| :--- | :--- | :--- |
| `students` | `student_id`, `name`, `major`, `password`, `is_paid`, `is_checked_in` | 存储学生全量信息及状态。 |
| `dorm_buildings` | `building_no`, `name` | 宿舍楼栋基础信息。 |
| `dorm_rooms` | `building_id`, `room_no`, `capacity`, `occupied_count`, `major` | **关键**：`major`字段用于算法筛选；`occupied_count`记录实时占用。 |
| `dorm_beds` | `room_id`, `bed_no`, `is_occupied`, `student_id` | 床位与入住学生的绑定关系。 |
| `qualification_modifications` | `student_id`, `field_name`, `old_value`, `new_value`, `status` | 存储学生发起的信息修改申请流程。 |
| `payment_records` | `student_id`, `fee_item_id`, `status`, `pay_time` | 记录缴费流水（模拟）。 |
| `admins` | `username`, `password` | 管理员登录凭证。 |

---

## 6. 核心业务算法详细说明

### 6.1 宿舍自动分配算法 (同专业尽量同房间)

**触发时机**：学生端点击“自动分配”按钮（前提：已缴费）。

**算法伪代码逻辑**：

```text
1. 获取当前学生的专业(Major)和所在楼栋(BuildingId)。
2. 查询 dorm_rooms 表：
   WHERE building_id = ? AND major = ? AND occupied_count < capacity
   ORDER BY occupied_count DESC (优先填满现有空位，减少碎片)
   LIMIT 1
3. IF 房间存在:
    在该房间的 dorm_beds 表中查找 is_occupied = false 的第一个床位。
    UPDATE 床位绑定学生ID，房间 occupied_count + 1。
   ELSE:
    在 dorm_rooms 中创建该专业的新房间（容量默认为4，房间号自动生成）。
    分配该新房间的第一个床位。
4. 返回分配结果给前端。
```

---

## 7. 接口设计规范 (API 概览)

### 7.1 统一返回格式
```json
{
  "code": 200,
  "message": "操作成功",
  "data": { ... }
}
```

### 7.2 关键核心接口列表

| 模块 | 方法 | 路径 | 描述 |
| :--- | :--- | :--- | :--- |
| **认证** | POST | `/api/auth/login` | 学号密码登录（传参：`captcha`固定6666） |
| **资格** | GET | `/api/student/qualification` | 获取当前学生信息 |
| **资格** | POST | `/api/student/qualification/apply` | 提交修改申请 (字段名/新值/原因) |
| **缴费** | POST | `/api/student/payment` | 模拟缴费 (传项目ID列表) |
| **宿舍** | POST | `/api/student/dorm/assign` | 执行自动分配算法 |
| **报到** | POST | `/api/student/checkin` | 确认现场报到 |
| **审核** | GET | `/api/admin/modifications/list` | 管理员分页获取待审核列表 |
| **审核** | PUT | `/api/admin/modifications/approve/{id}` | 审核通过/驳回 |
| **统计** | GET | `/api/admin/dashboard/stats` | 获取看板汇总数据及图表数据 |

---

## 8. 前端页面流程与交互约束

### 8.1 学生端四步报到流程
- 使用 **步骤条 (Step)** 组件展示进度。
- **约束**：用户不可跳跃未完成的步骤，且当前所处步骤由后端返回的 `current_step` 字段控制自动跳转。
- **操作反馈**：每一步操作成功（如缴费成功、分配成功）后，步骤条自动流转至下一步。

### 8.2 管理后台布局
- 采用 **左侧菜单 + 右侧内容区** 经典布局（`Container`）。
- **ECharts 统计**：看板顶部展示 4 个核心指标卡（总人数、已报到、缴费人数、入住人数）；下方分两列展示柱状图（学院报到率）和折线图（缴费趋势）。

---

## 9. 开发环境与部署指引 (本地)

| 项目 | 配置说明 |
| :--- | :--- |
| **后端启动** | IDE 运行 `SpringBootApplication` 主类，监听 `8080` 端口。 |
| **前端启动** | `npm install` 安装依赖，`npm run dev` 启动，访问 `http://localhost:5173`。 |
| **Vite 代理配置** | `vite.config.ts` 中配置 proxy：`{ '/api': { target: 'http://localhost:8080', changeOrigin: true } }`。 |
| **数据库初始化** | 执行阶段一生成的 `schema.sql` 创建库表结构，并插入基础管理员测试数据。 |

---

## 10. 附加约束与扩展预留

1. **验证码**：当前阶段仅做前端展示和固定校验，代码中预留 `CaptchaService` 空实现，便于后续升级为图形验证码。
2. **宿舍新建规则**：若某专业全部宿舍已满，系统会**自动创建新房间**，而不会跨专业混住，以保证管理清晰。
3. **密码重置**：管理员重置学生密码时，默认重置为 `身份证号后六位` 或固定值 `123456`（需在提示词中指定）。

---

*文档结束*